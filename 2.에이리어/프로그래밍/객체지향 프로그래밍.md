---
created: 2025-04-15 16:02
tags:
---

---
# 객체지향 프로그래밍

## **클래스 변수 vs 인스턴스 변수 차이**

- `self.population` → **각 객체(인스턴스)마다 따로 저장**됨.
- `Robot.population` → **모든 객체가 공유하는 값**이므로, 객체가 생성될 때마다 증가함.


## **`__init__`의 역할**
- **객체가 생성될 때 자동으로 호출되는 함수**로, **객체의 초기 상태를 설정**

```python
class 클래스이름:
    def __init__(self, 매개변수1, 매개변수2, ...):
        self.속성1 = 매개변수1
        self.속성2 = 매개변수2
```

## **예제1**
```python
class Car:
    pass  # 아무것도 정의하지 않음

car1 = Car()  # 객체 생성
car1.color = "red"  # 속성을 직접 추가
print(car1.color)  # red
```
*init을 쓰지 않으면 속성을 직접 설정해야 한다.*

## 상속

```python
class Robot:
    def speak(self):
        print("로봇이 말한다.")

    def move(self):
        print("로봇이 이동한다.")

    def charge(self):
        print("로봇이 충전한다.")

class CleanRobot(Robot):
    def broom(self):
        print("청소로봇이 먼지를 쓸어 담는다.")

#청소 로봇이 로봇의 클래스를 상속받는다.
clean = CleanRobot()
clean.move()
clean.broom()
```




---
# 링크 
## 🔗 아웃링크
```dataview
LIST WITHOUT ID outgoing
FROM ""
WHERE file = this.file
FLATTEN file.outlinks as outgoing
WHERE endswith(lower(outgoing.path), ".md")
SORT outgoing ASC
```
## 🔙 백링크
```dataview
LIST WITHOUT ID file.link
WHERE contains(file.outlinks, this.file.link)
SORT file.link ASC
```
